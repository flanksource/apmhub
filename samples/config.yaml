backends:
  - kubernetes:
      kubeconfig:
  - elasticsearch:
      # address: "http://localhost:9200"
      username: 
        value: "elastic"
      password: 
        value: "elastic"
      cloudID: 
        value: "apm:abcdefghijklmnopqrstuvwxyz"
      apiKey: 
        value: ""
      index: "test"
      query: '
        {
          "query": { 
            "match": { 
              "title": "{{.Title}}" 
            }
          }
        }'
